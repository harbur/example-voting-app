apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: worker
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - image: docker.pro.harbur.io/example-voting-app-worker:{{ .Chart.AppVersion }}
        name: worker
